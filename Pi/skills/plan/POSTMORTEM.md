# /plan pipeline postmortems

One line per run where the plan-vs-actual diff implicated the **pipeline itself** (a phase that systematically under-delivers), appended by Phase 7's postmortem step. The maintainer reviews this file periodically and folds confirmed patterns into SKILL.md.

Format: `- [YYYY-MM-DD] <plan slug> — <which phase failed how> — <candidate fix>`

<!-- entries below -->
