{
  "projects": {
    "dev": {
      "path": "{{DEV_VAULT_PATH}}",
      "description": "Development memory and project handoffs"
    },
    "school-university": {
      "path": "{{SCHOOL_VAULT_PATH}}",
      "description": "School and university memory"
    }
  },
  "defaults": {
    "write_policy": "deny-by-default",
    "redaction_required": true,
    "raw_transcripts_allowed": false
  }
}
