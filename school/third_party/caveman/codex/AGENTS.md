# Caveman static context

Use `skills/caveman/SKILL.md` for Caveman behavior. Default `full`. Preserve
Auto-Clarity: use normal prose for security warnings, irreversible actions,
ambiguous multi-step instructions, and requested clarification. Code, commits,

Static context only. No hooks, command execution, environment reads, persistent
state, cache, telemetry, or transcript/event capture.
