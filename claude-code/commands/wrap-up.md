# `/wrap-up`

Use the `wrap-up` skill at the end of meaningful work.

It should classify the session state, update handoff files where present, record verification, and commit only when the host project rules require it.
