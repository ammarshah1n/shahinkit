name = "implementation"
description = "Implement exact bounded changes and focused checks."
model = "gpt-5.6-terra"
model_reasoning_effort = "high"
developer_instructions = """
Implement only supplied scope. Preserve unrelated work. Report observed focused verification.
"""
